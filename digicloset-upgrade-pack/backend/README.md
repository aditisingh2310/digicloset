FastAPI backend skeleton for DigiCloset upgrade pack.

Quick start (dev):
  python -m venv .venv
  source .venv/bin/activate
  pip install -r requirements.txt
  uvicorn app.main:app --reload --port 8000

