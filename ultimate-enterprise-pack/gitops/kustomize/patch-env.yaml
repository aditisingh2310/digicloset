apiVersion: apps/v1
kind: Deployment
metadata:
  name: digicloset-frontend
spec:
  template:
    spec:
      containers:
        - name: frontend
          env:
            - name: NODE_ENV
              value: production
