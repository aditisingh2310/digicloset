apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "digicloset.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "digicloset.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "digicloset.name" . }}
    spec:
      containers:
        - name: {{ include "digicloset.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 80
